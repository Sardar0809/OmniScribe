# OmniScribe Environment Variables

# Place your Google AI Studio Gemini API key here (server-side only, never exposed to user)
GEMINI_API_KEY=

# Secret key for JWT session encryption (any strong password text)
JWT_SECRET=omniscribe_super_key_2026
